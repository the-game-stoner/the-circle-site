---
const SITE_NAME = "The-Circle";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Apply • Fantasy League • {SITE_NAME}</title>
    <meta name="description" content="Apply to join The-Circle Fantasy League." />
  </head>

  <body>
    <div class="page">
      <header class="top">
        <div class="container nav">
          <a class="brand" href="/"><span class="mark"></span><span>{SITE_NAME}</span></a>
          <nav class="links">
            <a href="/">Home</a>
            <a href="/discord">Discord</a>
            <a href="/league">League</a>
            <a href="/about">About</a>
          </nav>
        </div>
      </header>

      <main class="container">
        <section class="card">
          <div class="head">
            <h1>Apply to join the league</h1>
            <a class="mini" href="/fantasy">← Back to Fantasy hub</a>
          </div>

          <p class="lead">
            This league is invite/approval based. If a spot opens, applications help me choose someone who’ll actually show up,
            set a lineup, and keep it fun.
          </p>

          <div class="note">
            <strong>Right now:</strong> this form is a placeholder (no backend yet).
            When you’re ready, we’ll wire this to email, a database, or Discord verification.
          </div>

          <form class="form" action="#" method="post">
            <div class="row">
              <label>
                <span>Name</span>
                <input type="text" name="name" placeholder="Your name" autocomplete="name" />
              </label>

              <label>
                <span>Email</span>
                <input type="email" name="email" placeholder="you@example.com" autocomplete="email" />
              </label>
            </div>

            <div class="row">
              <label>
                <span>Discord username (optional)</span>
                <input type="text" name="discord" placeholder="username or @handle" />
              </label>

              <label>
                <span>Timezone</span>
                <input type="text" name="timezone" placeholder="e.g., Eastern" />
              </label>
            </div>

            <label>
              <span>Fantasy experience</span>
              <textarea name="experience" rows="4" placeholder="How long you’ve played, favorite format, etc."></textarea>
            </label>

            <label>
              <span>Why do you want in?</span>
              <textarea name="why" rows="4" placeholder="Quick pitch. What makes you a good league member?"></textarea>
            </label>

            <div class="cta">
              <button class="btn primary" type="button" disabled title="Backend not wired yet">
                Submit application (coming)
              </button>
              <a class="btn ghost" href="/league/rules">Rules & settings</a>
              <a class="btn ghost" href="/league/teams">Teams & owners</a>
            </div>

            <p class="fine">
              When you’re ready to make this real, easiest path is: Cloudflare Pages + Forms (3rd party) or a Worker that saves to D1.
            </p>
          </form>
        </section>
      </main>
    </div>

    <style is:global>
      html, body { height: 100%; }
      body{
        margin:0; min-height:100vh;
        font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;
        color:rgba(255,255,255,0.92);
        background-image:
          radial-gradient(900px 500px at 15% 10%, rgba(9,255,3,0.14), transparent 60%),
          radial-gradient(800px 450px at 85% 15%, rgba(120,170,255,0.18), transparent 55%),
          radial-gradient(700px 450px at 55% 95%, rgba(255,170,120,0.10), transparent 55%),
          linear-gradient(#070a0f, #070a0f);
        background-repeat:no-repeat;
        background-size:100% 100%;
        background-attachment:fixed;
      }
      .page{min-height:100vh;display:flex;flex-direction:column}
      main{flex:1}
      a{color:inherit;text-decoration:none}
      .container{max-width:1020px;margin:0 auto;padding:0 18px}
      .top{position:sticky;top:0;z-index:10;backdrop-filter:blur(10px);background:rgba(7,10,15,0.55);border-bottom:1px solid rgba(255,255,255,0.10)}
      .nav{display:flex;align-items:center;justify-content:space-between;padding:14px 18px;gap:14px}
      .brand{display:flex;gap:10px;align-items:center;font-weight:800;letter-spacing:-0.02em;white-space:nowrap}
      .mark{width:12px;height:12px;border-radius:999px;background:#09ff03;box-shadow:0 0 0 6px rgba(9,255,3,0.12), 0 0 28px rgba(9,255,3,0.25)}
      .links{display:flex;gap:14px;align-items:center}
      .links a{color:rgba(255,255,255,0.78);font-size:14px}
      .links a:hover{color:rgba(255,255,255,0.95)}
      .pill{padding:10px 12px;border-radius:999px;border:1px solid rgba(9,255,3,0.24);background:rgba(9,255,3,0.10);color:rgba(255,255,255,0.95)}
      .mobileJoin{display:none}
      .card{margin:28px 0;border:1px solid rgba(255,255,255,0.12);background:rgba(255,255,255,0.06);border-radius:18px;padding:20px;box-shadow:0 20px 60px rgba(0,0,0,0.45)}
      .head{display:flex;align-items:baseline;justify-content:space-between;gap:12px;flex-wrap:wrap}
      .mini{color:rgba(255,255,255,0.70);font-size:14px}
      .mini:hover{color:rgba(255,255,255,0.95)}
      h1{margin:0 0 10px;font-size:34px;letter-spacing:-0.02em}
      .lead{color:rgba(255,255,255,0.72);font-size:17px;line-height:1.6;max-width:86ch;margin:0 0 14px}
      .note{border:1px solid rgba(9,255,3,0.16);background:rgba(9,255,3,0.07);border-radius:16px;padding:12px 14px;color:rgba(255,255,255,0.80);margin:10px 0 14px}
      .form{margin-top:10px}
      label{display:block;margin:12px 0}
      label span{display:block;margin-bottom:6px;color:rgba(255,255,255,0.72);font-size:14px}
      input, textarea{
        width:100%;
        padding:12px 12px;
        border-radius:14px;
        border:1px solid rgba(255,255,255,0.14);
        background:rgba(255,255,255,0.03);
        color:rgba(255,255,255,0.92);
        outline:none;
      }
      input:focus, textarea:focus{border-color:rgba(9,255,3,0.35);box-shadow:0 0 0 4px rgba(9,255,3,0.10)}
      .row{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
      .cta{display:flex;gap:12px;flex-wrap:wrap;margin-top:14px}
      .btn{
        display:inline-flex;align-items:center;justify-content:center;
        padding:12px 16px;border-radius:14px;border:1px solid rgba(255,255,255,0.14);
        background:rgba(255,255,255,0.04);
        box-shadow:0 8px 25px rgba(0,0,0,0.22);
        transition:transform .12s ease, background .12s ease;
        cursor:pointer;
      }
      .btn:hover{transform:translateY(-1px);background:rgba(255,255,255,0.07)}
      .btn.primary{border-color:rgba(9,255,3,0.22);background:rgba(9,255,3,0.10)}
      .btn.ghost{background:rgba(255,255,255,0.03)}
      button.btn[disabled]{opacity:.55;cursor:not-allowed;transform:none}
      .fine{margin-top:14px;color:rgba(255,255,255,0.55);font-size:13px}
      @media (max-width:820px){.links{display:none}.mobileJoin{display:inline-flex}.row{grid-template-columns:1fr}}
    </style>
  </body>
</html>
