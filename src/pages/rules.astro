---
const SITE_NAME = "The-Circle";
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Rules • {SITE_NAME}</title>
    <meta name="description" content="Simple rules to keep The-Circle chill." />
  </head>

  <body>
    <header class="top">
      <div class="container nav">
        <a class="brand" href="/"><span class="dot"></span><span>{SITE_NAME}</span></a>
        <nav class="links">
          <a href="/about">About</a>
          <a href="/join">Join</a>
          <a class="pill" href="/">Home</a>
        </nav>
      </div>
    </header>

    <main class="container">
      <section class="card">
        <h1>Rules (simple on purpose)</h1>
        <p class="lead">The goal is a low-pressure place that stays comfortable to be in.</p>

        <ol class="list">
          <li><strong>Be normal and respectful.</strong> No harassment, hate, or targeting people.</li>
          <li><strong>No dating / sexual behavior.</strong> Keep it friendly and platonic.</li>
          <li><strong>No spam or self-promo.</strong> Ask first if you want to share something.</li>
          <li><strong>Don’t bring drama.</strong> Handle issues calmly or involve mods.</li>
          <li><strong>Use common sense.</strong> If you wouldn’t do it in a chill living room, don’t do it here.</li>
        </ol>

        <p class="fine">Rules can evolve as the server grows — the vibe stays the same.</p>
      </section>
    </main>

    <style>
      :global(body){margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,Arial,sans-serif;color:rgba(255,255,255,.92);
      background:radial-gradient(900px 500px at 15% 10%, rgba(120,170,255,.22), transparent 60%),
      radial-gradient(800px 450px at 85% 15%, rgba(140,255,190,.16), transparent 55%),
      radial-gradient(700px 450px at 55% 95%, rgba(255,170,120,.12), transparent 55%), #0b0f14;}
      a{color:inherit;text-decoration:none}
      .container{max-width:980px;margin:0 auto;padding:0 18px}
      .top{position:sticky;top:0;backdrop-filter:blur(10px);background:rgba(11,15,20,.55);border-bottom:1px solid rgba(255,255,255,.10);z-index:10}
      .nav{display:flex;align-items:center;justify-content:space-between;padding:14px 18px}
      .brand{display:flex;gap:10px;align-items:center;font-weight:700;letter-spacing:-0.02em}
      .dot{width:10px;height:10px;border-radius:999px;background:rgba(255,255,255,.85);box-shadow:0 0 0 6px rgba(255,255,255,.08)}
      .links{display:flex;gap:14px;align-items:center}
      .links a{color:rgba(255,255,255,.78);font-size:14px}
      .links a:hover{color:rgba(255,255,255,.95)}
      .pill{padding:10px 12px;border-radius:999px;border:1px solid rgba(255,255,255,.14);background:rgba(255,255,255,.06)}
      .card{margin:28px 0;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.06);border-radius:18px;padding:20px;box-shadow:0 20px 60px rgba(0,0,0,.45)}
      h1{margin:0 0 10px;font-size:34px;letter-spacing:-0.02em}
      .lead{color:rgba(255,255,255,.72);font-size:17px;line-height:1.6;max-width:75ch}
      .list{margin:14px 0 0 18px;color:rgba(255,255,255,.75);line-height:1.7}
      .list li{margin:10px 0}
      .fine{margin-top:14px;color:rgba(255,255,255,.55);font-size:13px}
    </style>
  </body>
</html>
