---
const SITE_NAME = "The-Circle";
const BRAND_TAGLINE = "The-Circle is the brand. Discord and Fantasy are the products.";
const DISCORD_INVITE = "https://discord.gg/9rPcHQZ7SH";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{SITE_NAME}</title>
    <meta
      name="description"
      content="The-Circle is a laid-back brand built around two things: a drop-in Discord hangout and a private fantasy football league hub."
    />
  </head>

  <body>
    <div class="page">
      <header class="top">
        <div class="container nav">
          <a class="brand" href="/">
            <span class="mark" aria-hidden="true"></span>
            <span class="name">{SITE_NAME}</span>
          </a>

          <nav class="links">
            <a href="/discord">Discord</a>
            <a href="/league">League</a>
            <a href="/about">About</a>
            <a href="/rules">Rules</a>
            <a class="pill" href="/join">Join</a>
          </nav>

          <a class="pill mobileJoin" href="/join">Join</a>
        </div>
      </header>

      <main class="container">
        <section class="hero">
          <div class="heroGrid">
            <div class="heroLeft">
              <div class="badge">Brand-first • product-driven</div>

              <h1>
                {SITE_NAME}
                <span class="glowDot" aria-hidden="true">•</span>
              </h1>

              <p class="sub">
                {BRAND_TAGLINE}
                <br />
                A place you can drop into when you’re bored — hang out, game, watch stuff, or just talk.
              </p>

              <div class="cta">
                <a class="btn primary" href="/discord">Explore Discord</a>
                <a class="btn ghost" href="/league">Explore League</a>
                <a class="btn ghost" href={DISCORD_INVITE} target="_blank" rel="noreferrer">Join Discord</a>
              </div>

              <p class="fine">
                No clout. No pressure. No “be active every day” nonsense. Just a clean setup and a chill vibe.
              </p>
            </div>

            <div class="heroRight" aria-hidden="true">
              <div class="orb orbA"></div>
              <div class="orb orbB"></div>
              <div class="panel">
                <div class="panelTop">
                  <span class="dot"></span>
                  <span class="dot"></span>
                  <span class="dot"></span>
                </div>

                <div class="panelBody">
                  <div class="stat">
                    <span class="k">Drop-in energy</span>
                    <span class="v">Low pressure</span>
                  </div>
                  <div class="stat">
                    <span class="k">Channels</span>
                    <span class="v">Organized</span>
                  </div>
                  <div class="stat">
                    <span class="k">Voice/stream</span>
                    <span class="v">Boosted quality</span>
                  </div>
                  <div class="stat">
                    <span class="k">League hub</span>
                    <span class="v">Private & clean</span>
                  </div>
                </div>

                <div class="panelFoot">
                  <span class="pill2">Come & go</span>
                  <span class="pill2">Adult-friendly</span>
                  <span class="pill2">No drama</span>
                </div>
              </div>
            </div>
          </div>
        </section>

        <section class="card">
          <div class="cardHead">
            <h2>Choose your lane</h2>
            <p class="note">
              The-Circle is the umbrella. Pick what you’re here for — or use both.
            </p>
          </div>

          <div class="grid">
            <article class="tile">
              <div class="tileTop">
                <span class="chip">Product</span>
                <h3>Discord Hangout</h3>
              </div>

              <p>
                A well-built Discord that isn’t centered around a creator, product, or one game.
                Drop in for voice, games, music, and movie nights — or just background company.
              </p>

              <div class="tileCta">
                <a class="btn primary" href="/discord">See how it works</a>
                <a class="btn ghost" href={DISCORD_INVITE} target="_blank" rel="noreferrer">Join now</a>
              </div>
            </article>

            <article class="tile">
              <div class="tileTop">
                <span class="chip">Product</span>
                <h3>Fantasy League</h3>
              </div>

              <p>
                A private league hub for the league you run: teams & owners, history, rules/settings,
                and applications when a spot opens.
              </p>

              <div class="tileCta">
                <a class="btn primary" href="/league">League hub</a>
                <a class="btn ghost" href="/league/apply">Apply</a>
              </div>
            </article>
          </div>
        </section>

        <section class="card">
          <div class="cardHead">
            <h2>The vibe</h2>
            <p class="note">
              Most “friends servers” are messy and fall apart. Most big servers are noisy and impersonal.
              The-Circle is a reliable middle ground: small enough to feel human, set up well enough to actually use.
            </p>
          </div>

          <div class="pills">
            <span class="pill2">Low pressure</span>
            <span class="pill2">No cliques</span>
            <span class="pill2">Organized</span>
            <span class="pill2">Good voice quality</span>
            <span class="pill2">Come & go anytime</span>
            <span class="pill2">Adult-friendly energy</span>
          </div>
        </section>

        <section class="card final">
          <h2>Ready?</h2>
          <p class="note">
            If you’re already on Discord a lot and want a reliable place to hang out, start here.
            You can lurk first — nobody’s going to hassle you.
          </p>
          <div class="cta">
            <a class="btn primary big" href="/join">Join The-Circle</a>
            <a class="btn ghost big" href={DISCORD_INVITE} target="_blank" rel="noreferrer">Join Discord</a>
          </div>
        </section>
      </main>

      <footer class="footer">
        <div class="container foot">
          <span>© {new Date().getFullYear()} {SITE_NAME}</span>
          <span class="muted">Brand • Community • Products</span>
        </div>
      </footer>
    </div>

    <style is:global>
      html, body { height: 100%; }

      body{
        margin:0;
        min-height:100vh;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        color: rgba(255,255,255,0.92);

        background-image:
          radial-gradient(900px 500px at 15% 10%, rgba(9,255,3,0.14), transparent 60%),
          radial-gradient(800px 450px at 85% 15%, rgba(120,170,255,0.18), transparent 55%),
          radial-gradient(700px 450px at 55% 95%, rgba(255,170,120,0.10), transparent 55%),
          linear-gradient(#070a0f, #070a0f);

        background-repeat:no-repeat;
        background-size:100% 100%;
        background-attachment:fixed;
      }

      a{ color:inherit; text-decoration:none; }
      .container{ max-width: 1020px; margin: 0 auto; padding: 0 18px; }

      .page{ min-height:100vh; display:flex; flex-direction:column; }
      main{ flex:1; }

      /* Top Nav */
      .top{
        position: sticky; top: 0; z-index: 10;
        backdrop-filter: blur(10px);
        background: rgba(7,10,15,0.55);
        border-bottom: 1px solid rgba(255,255,255,0.10);
      }
      .nav{ display:flex; align-items:center; justify-content:space-between; padding:14px 18px; gap:14px; }
      .brand{ display:flex; align-items:center; gap:10px; font-weight:900; letter-spacing:-0.02em; white-space:nowrap; }
      .mark{
        width:12px;height:12px;border-radius:999px;background:#09ff03;
        box-shadow: 0 0 0 6px rgba(9,255,3,0.12), 0 0 28px rgba(9,255,3,0.25);
      }
      .links{ display:flex; gap:14px; align-items:center; }
      .links a{ color: rgba(255,255,255,0.78); font-size:14px; }
      .links a:hover{ color: rgba(255,255,255,0.95); }

      .pill{
        padding:10px 12px;border-radius:999px;
        border: 1px solid rgba(9,255,3,0.24);
        background: rgba(9,255,3,0.10);
        color: rgba(255,255,255,0.95);
        box-shadow: 0 0 0 1px rgba(9,255,3,0.08) inset;
      }
      .mobileJoin{ display:none; }

      /* Hero */
      .hero{ padding: 46px 0 18px; }
      .heroGrid{ display:grid; grid-template-columns: 1.2fr 0.8fr; gap: 18px; align-items: stretch; }

      .badge{
        display:inline-flex; align-items:center;
        padding: 8px 12px;
        border: 1px solid rgba(255,255,255,0.14);
        border-radius: 999px;
        background: rgba(255,255,255,0.04);
        color: rgba(255,255,255,0.78);
        font-size: 13px;
        letter-spacing: 0.01em;
      }

      h1{
        margin: 14px 0 10px;
        font-size: 56px;
        letter-spacing: -0.03em;
        line-height: 1.02;
      }

      .glowDot{
        color:#09ff03;
        text-shadow: 0 0 24px rgba(9,255,3,0.35);
        margin-left: 6px;
      }

      .sub{
        margin: 0;
        color: rgba(255,255,255,0.74);
        font-size: 18px;
        line-height: 1.7;
        max-width: 70ch;
      }

      .cta{ display:flex; gap:12px; flex-wrap:wrap; margin-top: 16px; }

      .btn{
        display:inline-flex; align-items:center; justify-content:center;
        padding: 12px 16px;
        border-radius: 14px;
        border: 1px solid rgba(255,255,255,0.14);
        background: rgba(255,255,255,0.04);
        box-shadow: 0 10px 30px rgba(0,0,0,0.22);
        transition: transform .14s ease, background .14s ease, border-color .14s ease;
      }
      .btn:hover{ transform: translateY(-1px); background: rgba(255,255,255,0.07); }
      .btn.primary{ border-color: rgba(9,255,3,0.22); background: rgba(9,255,3,0.10); }
      .btn.ghost{ background: rgba(255,255,255,0.03); }
      .btn.big{ padding: 13px 18px; border-radius: 16px; }

      .fine{
        margin-top: 14px;
        color: rgba(255,255,255,0.55);
        font-size: 13px;
      }

      /* Hero right panel */
      .heroRight{ position: relative; }
      .orb{
        position:absolute; filter: blur(14px);
        opacity: 0.9;
        border-radius: 999px;
      }
      .orbA{ width: 210px; height: 210px; left: -10px; top: 20px; background: rgba(9,255,3,0.22); }
      .orbB{ width: 240px; height: 240px; right: -20px; bottom: 10px; background: rgba(120,170,255,0.20); }

      .panel{
        position: relative;
        height: 100%;
        border-radius: 20px;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(255,255,255,0.05);
        box-shadow: 0 30px 80px rgba(0,0,0,0.45);
        overflow: hidden;
        padding: 14px;
      }

      .panelTop{ display:flex; gap:8px; padding: 6px 6px 12px; }
      .dot{ width:10px; height:10px; border-radius:999px; background: rgba(255,255,255,0.18); }

      .panelBody{ display:grid; gap: 10px; padding: 6px; }
      .stat{
        display:flex; justify-content:space-between; gap:12px;
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 16px;
        padding: 12px 12px;
        background: rgba(255,255,255,0.03);
      }
      .k{ color: rgba(255,255,255,0.65); font-size: 13px; }
      .v{ color: rgba(255,255,255,0.92); font-weight: 700; }

      .panelFoot{ display:flex; flex-wrap:wrap; gap: 8px; padding: 12px 6px 4px; }

      /* Cards */
      .card{
        margin: 18px 0;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(255,255,255,0.06);
        border-radius: 20px;
        padding: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.45);
      }

      .cardHead{ display:flex; align-items:baseline; justify-content:space-between; gap: 12px; flex-wrap:wrap; }
      h2{ margin: 0 0 8px; font-size: 22px; letter-spacing: -0.02em; }
      h3{ margin: 0; font-size: 18px; }
      .note{ margin: 0; color: rgba(255,255,255,0.70); line-height: 1.6; max-width: 92ch; }

      .grid{ display:grid; grid-template-columns: repeat(2, minmax(0,1fr)); gap: 12px; margin-top: 14px; }
      .tile{
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 18px;
        padding: 16px;
        background: rgba(255,255,255,0.03);
      }
      .tile p{ margin: 10px 0 0; color: rgba(255,255,255,0.72); line-height: 1.6; }

      .tileTop{ display:flex; align-items:center; justify-content:space-between; gap: 10px; }
      .chip{
        padding: 6px 10px;
        border-radius: 999px;
        border: 1px solid rgba(9,255,3,0.20);
        background: rgba(9,255,3,0.08);
        color: rgba(255,255,255,0.82);
        font-size: 12px;
      }

      .tileCta{ display:flex; gap: 10px; flex-wrap:wrap; margin-top: 14px; }

      .pills{ display:flex; flex-wrap:wrap; gap: 10px; margin-top: 12px; }
      .pill2{
        padding: 8px 12px;
        border-radius: 999px;
        border: 1px solid rgba(255,255,255,0.14);
        background: rgba(255,255,255,0.03);
        color: rgba(255,255,255,0.78);
        font-size: 13px;
      }

      .final{ margin-bottom: 28px; }

      /* Footer */
      .footer{
        border-top: 1px solid rgba(255,255,255,0.10);
        background: rgba(7,10,15,0.35);
        backdrop-filter: blur(10px);
      }
      .foot{ display:flex; justify-content:space-between; gap:12px; padding: 16px 18px; flex-wrap:wrap; }
      .muted{ color: rgba(255,255,255,0.55); }

      @media (max-width: 900px){
        .heroGrid{ grid-template-columns: 1fr; }
        h1{ font-size: 44px; }
        .heroRight{ display:none; }
        .grid{ grid-template-columns: 1fr; }
      }
      @media (max-width: 820px){
        .links{ display:none; }
        .mobileJoin{ display:inline-flex; }
      }
    </style>
  </body>
</html>
