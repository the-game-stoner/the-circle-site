---
const SITE_NAME = "The-Circle";
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{SITE_NAME}</title>
    <meta
      name="description"
      content="Welcome to The-Circle — a chill place to hang out, game, talk, and spend time with people."
    />
  </head>

  <body>
    <div class="page">
      <header class="top">
        <div class="container nav">
          <a class="brand" href="/">
            <span class="mark" aria-hidden="true"></span>
            <span class="name">{SITE_NAME}</span>
          </a>

          <nav class="links">
            <a href="/discord">Discord</a>
            <a href="/league">League</a>
            <a href="/about">About</a>
            <a href="/rules">Rules</a>
            <a class="pill" href="/join">Join</a>
          </nav>

          <a class="pill mobileJoin" href="/join">Join</a>
        </div>
      </header>

      <main class="container">
        <section class="hero">
          <div class="welcome">
            <h1>
              Welcome to {SITE_NAME}.xyz
            </h1>

            <p class="sub">
              This is a laid-back corner of the internet for people who like having others around.
              Sometimes that means gaming together, sometimes it’s music, movies, or just sitting in voice while you do your own thing.
            </p>

            <p class="sub">
              There’s no pressure to be “active,” no cliques to break into, and no expectations.
              It’s just a place you can drop into when you feel like it.
            </p>

            <p class="fine">
              Take a look around and see if it feels right.
            </p>
          </div>
        </section>

        <section class="card">
          <h2>What’s here</h2>
          <p class="note">
            You don’t have to use everything. Pick what you’re into.
          </p>

          <div class="grid">
            <article class="tile">
              <div class="tileTop">
                <h3>Discord Hangout</h3>
                <span class="chip">Live</span>
              </div>
              <p>
                A clean, organized Discord for hanging out — voice, games, music, and movie nights.
                Built to be low pressure and easy to use.
              </p>
              <div class="tileCta">
                <a class="btn primary" href="/discord">Learn more</a>
                <a class="btn ghost" href="/join">Join</a>
              </div>
            </article>

            <article class="tile">
              <div class="tileTop">
                <h3>Fantasy League Hub</h3>
                <span class="chip">Live</span>
              </div>
              <p>
                A private home base for the fantasy league — teams, history, rules, and applications when a spot opens.
              </p>
              <div class="tileCta">
                <a class="btn primary" href="/league">Open hub</a>
                <a class="btn ghost" href="/league/teams">Teams</a>
              </div>
            </article>

            <article class="tile">
              <div class="tileTop">
                <h3>Coming soon</h3>
                <span class="chip soft">WIP</span>
              </div>
              <p>
                More Circle stuff as it grows — events, tools, and whatever ends up being useful.
              </p>
              <div class="tileCta">
                <a class="btn ghost" href="/about">Why this exists</a>
                <a class="btn ghost" href="/blog">Progress posts</a>
              </div>
            </article>
          </div>
        </section>
      </main>

      <footer class="footer">
        <div class="container foot">
          <span>© {new Date().getFullYear()} {SITE_NAME}</span>
          <span class="muted">A place to hang out</span>
        </div>
      </footer>
    </div>

    <style is:global>
      html, body { height: 100%; }

      body{
        margin:0;
        min-height:100vh;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        color: rgba(255,255,255,0.92);

        background-image:
          radial-gradient(900px 500px at 15% 10%, rgba(9,255,3,0.14), transparent 60%),
          radial-gradient(800px 450px at 85% 15%, rgba(120,170,255,0.18), transparent 55%),
          radial-gradient(700px 450px at 55% 95%, rgba(255,170,120,0.10), transparent 55%),
          linear-gradient(#070a0f, #070a0f);

        background-repeat:no-repeat;
        background-size:100% 100%;
        background-attachment:fixed;
      }

      a{ color:inherit; text-decoration:none; }
      .container{ max-width: 1020px; margin: 0 auto; padding: 0 18px; }

      .page{ min-height:100vh; display:flex; flex-direction:column; }
      main{ flex:1; }

      /* Top nav */
      .top{
        position: sticky;
        top: 0;
        z-index: 10;
        backdrop-filter: blur(10px);
        background: rgba(7,10,15,0.55);
        border-bottom: 1px solid rgba(255,255,255,0.10);
      }

      .nav{
        display:flex;
        align-items:center;
        justify-content:space-between;
        padding:14px 18px;
        gap:14px;
      }

      .brand{
        display:flex;
        align-items:center;
        gap:10px;
        font-weight:900;
        letter-spacing:-0.02em;
        white-space:nowrap;
      }

      .mark{
        width:12px;
        height:12px;
        border-radius:999px;
        background:#09ff03;
        box-shadow:
          0 0 0 6px rgba(9,255,3,0.12),
          0 0 28px rgba(9,255,3,0.25);
      }

      .links{ display:flex; gap:14px; align-items:center; }
      .links a{ color: rgba(255,255,255,0.78); font-size:14px; }
      .links a:hover{ color: rgba(255,255,255,0.95); }

      .pill{
        padding:10px 12px;
        border-radius:999px;
        border: 1px solid rgba(9,255,3,0.24);
        background: rgba(9,255,3,0.10);
        color: rgba(255,255,255,0.95);
        box-shadow: 0 0 0 1px rgba(9,255,3,0.08) inset;
      }

      .mobileJoin{ display:none; }

      /* Hero */
      .hero{ padding: 44px 0 16px; }

      .welcome{
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 22px;
        background: rgba(255,255,255,0.06);
        box-shadow: 0 24px 70px rgba(0,0,0,0.45);
        padding: 22px;
      }

      h1{
        margin: 0 0 12px;
        font-size: 52px;
        letter-spacing: -0.03em;
        line-height: 1.05;
      }

      .glowDot{
        color:#09ff03;
        text-shadow: 0 0 24px rgba(9,255,3,0.35);
        margin-left: 6px;
      }

      .sub{
        margin: 0 0 12px;
        color: rgba(255,255,255,0.74);
        font-size: 18px;
        line-height: 1.7;
        max-width: 86ch;
      }

      .fine{
        margin-top: 8px;
        color: rgba(255,255,255,0.55);
        font-size: 13px;
      }

      /* Cards */
      .card{
        margin: 18px 0;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(255,255,255,0.06);
        border-radius: 20px;
        padding: 20px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.45);
      }

      h2{ margin: 0 0 8px; font-size: 22px; letter-spacing: -0.02em; }
      h3{ margin: 0; font-size: 18px; }

      .note{
        margin: 0;
        color: rgba(255,255,255,0.70);
        line-height: 1.6;
      }

      .grid{
        display:grid;
        grid-template-columns: repeat(3, minmax(0,1fr));
        gap: 12px;
        margin-top: 14px;
      }

      .tile{
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: 18px;
        padding: 16px;
        background: rgba(255,255,255,0.03);
      }

      .tile p{
        margin: 10px 0 0;
        color: rgba(255,255,255,0.72);
        line-height: 1.6;
      }

      .tileTop{
        display:flex;
        align-items:center;
        justify-content:space-between;
        gap: 10px;
      }

      .chip{
        padding: 6px 10px;
        border-radius: 999px;
        border: 1px solid rgba(9,255,3,0.20);
        background: rgba(9,255,3,0.08);
        color: rgba(255,255,255,0.82);
        font-size: 12px;
      }

      .chip.soft{
        border-color: rgba(255,255,255,0.16);
        background: rgba(255,255,255,0.04);
      }

      .tileCta{
        display:flex;
        gap: 10px;
        flex-wrap:wrap;
        margin-top: 14px;
      }

      /* Footer */
      .footer{
        border-top: 1px solid rgba(255,255,255,0.10);
        background: rgba(7,10,15,0.35);
        backdrop-filter: blur(10px);
      }

      .foot{
        display:flex;
        justify-content:space-between;
        gap:12px;
        padding: 16px 18px;
        flex-wrap:wrap;
      }

      .muted{ color: rgba(255,255,255,0.55); }

      @media (max-width: 980px){
        .grid{ grid-template-columns: 1fr; }
        h1{ font-size: 44px; }
      }

      @media (max-width: 820px){
        .links{ display:none; }
        .mobileJoin{ display:inline-flex; }
      }
    </style>
  </body>
</html>
