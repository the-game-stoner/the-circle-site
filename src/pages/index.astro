---
const SITE_NAME = "The-Circle";
const year = new Date().getFullYear();
---

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{SITE_NAME}</title>
    <link rel="icon" href="/favicon.png" type="image/png" />
    <meta
      name="description"
      content="Welcome to The-Circle — a chill place to hang out, game, talk, and spend time with people."
    />
  </head>

  <body>
    <div class="page">
      <header class="top">
        <div class="container nav">
          <a class="brand" href="/" aria-label={`${SITE_NAME} Home`}>
            <img src="/favicon.png" alt="" class="logo" />
            <span class="name">{SITE_NAME}</span>
          </a>

          <nav class="links" aria-label="Primary">
            <a href="/discord" class="navLink">Discord</a>
            <a href="/games" class="navLink">Games</a>
            <a href="/league" class="navLink">League</a>
            <a href="/Music" class="navLink">Music</a>
            <a href="/about" class="navLink">About</a>
            <a href="/blog" class="navLink">Blog</a>
          </nav>

          {/* Mobile menu (no JS) */}
          <details class="menu">
            <summary class="menuBtn" aria-label="Open menu">
              <span class="menuIcon" aria-hidden="true"></span>
            </summary>
            <div class="menuPanel" role="menu">
              <a href="/discord" role="menuitem">Discord</a>
              <a href="/games" role="menuitem">Games</a>
              <a href="/league" role="menuitem">League</a>
              <a href="/Music" role="menuitem">Music</a>
              <a href="/about" role="menuitem">About</a>
              <a href="/blog" role="menuitem">Blog</a>
            </div>
          </details>
        </div>
      </header>

      <main class="container">
        <section class="hero">
          <div class="welcome">
            <div class="kicker">
              <span class="pill">Community projects</span>
              <span class="dot" aria-hidden="true"></span>
              <span class="pill soft">No ads • No memberships</span>
            </div>

            <h1>Welcome to <span class="accent">{SITE_NAME}.xyz</span></h1>

            <p class="sub">
              The-Circle is a collection of community projects created and run by TGS.
              The goal is simple: build fun, useful, and chill things for people to enjoy together online.
            </p>

            <p class="sub">
              Some of those projects are social, some are game-related, and others are just ideas that felt worth building.
              Everything here is free — no ads, no memberships, and no hidden catch.
            </p>

            <div class="heroCtas">
              <a class="btn primary" href="/discord">Join the Discord</a>
              <a class="btn" href="/Music">Browse Music</a>
            </div>

            <p class="fine">Take a look around and see what’s here.</p>
          </div>
        </section>

        <section class="card">
          <div class="cardHead">
            <div>
              <h2>Projects</h2>
              <p class="note">Check out our current projects.</p>
            </div>
          </div>

          {/* Row 1: 3 tiles (originals) */}
          <div class="grid grid3">
            <article class="tile">
              <div class="tileTop">
                <h3>The-Circle Discord</h3>
                <span class="chip live">Live</span>
              </div>
              <p>
                A clean, organized Discord for hanging out — voice, games, music, and movie nights.
                Built to be low pressure and easy to use.
              </p>
              <div class="tileCta">
                <a class="btn primary" href="/discord">Learn More</a>
              </div>
            </article>

            <article class="tile">
              <div class="tileTop">
                <h3>The-Circle League</h3>
                <span class="chip seasonal">Seasonal</span>
              </div>
              <p>
                Our Private & Public fantasy football leagues — teams, history, rules, and applications when a spot opens.
              </p>
              <div class="tileCta">
                <a class="btn primary" href="/league">Learn More</a>
              </div>
            </article>

            <article class="tile">
              <div class="tileTop">
                <h3>The-Circle Music</h3>
                <span class="chip new">New</span>
              </div>
              <p>
                Original music by TGS — country, rap, and rock/metal. A clean library where people can listen right on the site.
              </p>
              <div class="tileCta">
                <a class="btn primary" href="/Music">Learn More</a>
              </div>
            </article>
          </div>

          {/* Row 2: 2 tiles (added) */}
          <div class="grid grid2">
            <article class="tile">
              <div class="tileTop">
                <h3>The-Circle Games</h3>
                <span class="chip games">New</span>
              </div>
              <p>
                Interactive games built for the community. Start with Circle Cards — create a room,
                share a link, submit answers, vote, repeat.
              </p>
              <div class="tileCta">
                <a class="btn primary" href="/games">Open Games</a>
              </div>
            </article>

            <article class="tile">
              <div class="tileTop">
                <h3>More Coming Soon</h3>
                <span class="chip soon">Soon</span>
              </div>
              <p>
                More community projects are in the works — games, tools, and random ideas that are actually fun to use.
                This section will keep growing over time.
              </p>
              <div class="tileCta">
                <a class="btn" href="/games">See What’s New</a>
              </div>
            </article>
          </div>
        </section>
      </main>

      <footer class="footer">
        <div class="container foot">
          <span>© {year} {SITE_NAME}</span>
          <span class="muted">You’re either in The-Circle or you’re not</span>
        </div>
      </footer>
    </div>

    <style is:global>
      :root{
        --bg: #070a0f;
        --panel: rgba(255,255,255,0.06);
        --panel2: rgba(255,255,255,0.04);
        --border: rgba(255,255,255,0.12);
        --text: rgba(255,255,255,0.92);
        --muted: rgba(255,255,255,0.68);
        --muted2: rgba(255,255,255,0.55);

        --accent: #09ff03;
        --accentSoft: rgba(9,255,3,0.14);

        --r-lg: 22px;
        --r-md: 18px;
        --shadow: 0 24px 70px rgba(0,0,0,0.45);
        --shadow2: 0 18px 44px rgba(0,0,0,0.35);

        --focus: 0 0 0 3px rgba(9,255,3,0.25);
      }

      *{ box-sizing: border-box; }
      html, body { height: 100%; }
      body{
        margin:0;
        min-height:100vh;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
        color: var(--text);
        background:
          radial-gradient(900px 500px at 15% 10%, rgba(9,255,3,0.16), transparent 62%),
          radial-gradient(800px 450px at 85% 15%, rgba(120,170,255,0.18), transparent 58%),
          radial-gradient(700px 450px at 55% 95%, rgba(255,170,120,0.10), transparent 58%),
          linear-gradient(var(--bg), var(--bg));
        background-repeat:no-repeat;
        background-size:100% 100%;
        background-attachment:fixed;
      }

      a{ color:inherit; text-decoration:none; }
      .container{ max-width: 1060px; margin: 0 auto; padding: 0 18px; }

      .page{ min-height:100vh; display:flex; flex-direction:column; }
      main{ flex:1; }

      /* Top nav */
      .top{
        position: sticky;
        top: 0;
        z-index: 10;
        backdrop-filter: blur(12px);
        background: rgba(7,10,15,0.62);
        border-bottom: 1px solid rgba(255,255,255,0.10);
      }

      .nav{
        display:flex;
        align-items:center;
        justify-content:space-between;
        padding:14px 0;
        gap:14px;
      }

      .brand{
        display:flex;
        align-items:center;
        gap:10px;
        font-weight:900;
        letter-spacing:-0.02em;
        white-space:nowrap;
      }

      .logo{
        width:30px;
        height:30px;
        display:block;
        filter: drop-shadow(0 0 10px rgba(9,255,3,0.18));
      }

      .name{
        font-size: 15px;
        opacity: 0.95;
      }

      .links{ display:flex; gap:16px; align-items:center; }
      .navLink{
        position: relative;
        color: rgba(255,255,255,0.80);
        font-size:14px;
        padding: 8px 8px;
        border-radius: 12px;
        transition: background .12s ease, color .12s ease;
      }
      .navLink:hover{
        color: rgba(255,255,255,0.96);
        background: rgba(255,255,255,0.06);
      }
      .navLink:focus-visible{
        outline: none;
        box-shadow: var(--focus);
      }

      /* Mobile menu */
      .menu{ display:none; position: relative; }
      .menuBtn{
        list-style: none;
        cursor: pointer;
        user-select: none;
        width: 44px;
        height: 44px;
        border-radius: 14px;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(255,255,255,0.04);
        display:grid;
        place-items:center;
      }
      .menuBtn::-webkit-details-marker{ display:none; }
      .menuIcon{
        width: 18px;
        height: 2px;
        background: rgba(255,255,255,0.85);
        position: relative;
        border-radius: 999px;
      }
      .menuIcon::before,
      .menuIcon::after{
        content:"";
        position:absolute;
        left:0;
        width: 18px;
        height: 2px;
        background: rgba(255,255,255,0.85);
        border-radius: 999px;
      }
      .menuIcon::before{ top:-6px; }
      .menuIcon::after{ top:6px; }

      .menuPanel{
        position:absolute;
        right:0;
        top:56px;
        width: 220px;
        padding: 10px;
        border-radius: 16px;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(7,10,15,0.92);
        backdrop-filter: blur(12px);
        box-shadow: var(--shadow2);
      }
      .menuPanel a{
        display:block;
        padding: 10px 10px;
        border-radius: 12px;
        color: rgba(255,255,255,0.86);
      }
      .menuPanel a:hover{
        background: rgba(255,255,255,0.06);
        color: rgba(255,255,255,0.96);
      }

      /* Hero */
      .hero{ padding: 46px 0 18px; }

      .welcome{
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: var(--r-lg);
        background: linear-gradient(180deg, rgba(255,255,255,0.08), rgba(255,255,255,0.05));
        box-shadow: var(--shadow);
        padding: 26px 22px;
        text-align:center;
        position: relative;
        overflow:hidden;
      }
      .welcome::before{
        content:"";
        position:absolute;
        inset:-2px;
        background:
          radial-gradient(700px 220px at 50% 0%, rgba(9,255,3,0.14), transparent 60%),
          radial-gradient(500px 200px at 20% 110%, rgba(120,170,255,0.10), transparent 62%);
        pointer-events:none;
      }
      .welcome > *{ position: relative; }

      .kicker{
        display:flex;
        align-items:center;
        justify-content:center;
        gap:10px;
        margin-bottom: 12px;
        flex-wrap:wrap;
      }
      .pill{
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(255,255,255,0.04);
        padding: 6px 10px;
        border-radius: 999px;
        font-size: 12px;
        color: rgba(255,255,255,0.78);
      }
      .pill.soft{
        border-color: rgba(9,255,3,0.25);
        background: rgba(9,255,3,0.08);
      }
      .dot{
        width: 5px;
        height: 5px;
        border-radius: 999px;
        background: rgba(255,255,255,0.55);
      }

      h1{
        margin: 0 0 10px;
        font-size: 54px;
        letter-spacing: -0.04em;
        line-height: 1.05;
      }
      .accent{
        text-shadow: 0 0 18px rgba(9,255,3,0.20);
      }

      .sub{
        margin: 0 auto 12px;
        color: var(--muted);
        font-size: 18px;
        line-height: 1.75;
        max-width: 86ch;
      }

      .heroCtas{
        display:flex;
        gap: 10px;
        justify-content:center;
        flex-wrap:wrap;
        margin-top: 14px;
      }

      .fine{
        margin: 10px auto 0;
        color: var(--muted2);
        font-size: 13px;
      }

      /* Cards */
      .card{
        margin: 18px 0 26px;
        border: 1px solid rgba(255,255,255,0.12);
        background: rgba(255,255,255,0.06);
        border-radius: 20px;
        padding: 20px;
        box-shadow: var(--shadow2);
      }

      .cardHead{
        display:flex;
        align-items:flex-end;
        justify-content:space-between;
        gap: 12px;
        margin-bottom: 14px;
      }

      h2{ margin: 0 0 6px; font-size: 22px; letter-spacing: -0.01em; }
      .note{ margin: 0; color: var(--muted); }

      .grid{
        display:grid;
        gap: 12px;
      }

      /* Row 1: 3 columns */
      .grid3{
        grid-template-columns: repeat(3, minmax(0, 1fr));
      }

      /* Row 2: 2 columns */
      .grid2{
        margin-top: 12px;
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .tile{
        border: 1px solid rgba(255,255,255,0.12);
        border-radius: var(--r-md);
        padding: 16px;
        background: linear-gradient(180deg, rgba(255,255,255,0.05), rgba(255,255,255,0.03));
        transition: transform .14s ease, box-shadow .14s ease, border-color .14s ease;
        position: relative;
        overflow:hidden;
      }
      .tile::after{
        content:"";
        position:absolute;
        inset: -2px;
        background: radial-gradient(500px 200px at 20% 0%, rgba(9,255,3,0.10), transparent 55%);
        opacity: 0;
        transition: opacity .14s ease;
        pointer-events:none;
      }
      .tile:hover{
        transform: translateY(-3px);
        box-shadow: 0 18px 40px rgba(0,0,0,0.38);
        border-color: rgba(255,255,255,0.18);
      }
      .tile:hover::after{ opacity: 1; }
      .tile > *{ position: relative; }

      .tileTop{
        display:flex;
        align-items:center;
        justify-content:space-between;
        gap: 10px;
      }

      h3{
        margin:0;
        font-size: 16px;
        letter-spacing: -0.01em;
      }

      .tile p{
        margin: 10px 0 0;
        color: rgba(255,255,255,0.72);
        line-height: 1.65;
        font-size: 14.5px;
      }

      .chip{
        padding: 6px 10px;
        border-radius: 999px;
        font-size: 12px;
        border: 1px solid rgba(255,255,255,0.14);
        background: rgba(255,255,255,0.04);
        color: rgba(255,255,255,0.82);
        white-space:nowrap;
      }
      .chip.live{
        border-color: rgba(9,255,3,0.28);
        background: rgba(9,255,3,0.10);
      }
      .chip.seasonal{
        border-color: rgba(120,170,255,0.26);
        background: rgba(120,170,255,0.10);
      }
      .chip.new{
        border-color: rgba(255,170,120,0.22);
        background: rgba(255,170,120,0.10);
      }
      .chip.games{
        border-color: rgba(9,255,3,0.34);
        background: rgba(9,255,3,0.10);
      }
      .chip.soon{
        border-color: rgba(255,255,255,0.18);
        background: rgba(255,255,255,0.04);
        color: rgba(255,255,255,0.72);
      }

      .tileCta{ margin-top: 14px; }

      /* Buttons */
      .btn{
        display:inline-flex;
        align-items:center;
        justify-content:center;
        gap: 8px;
        padding: 12px 18px;
        border-radius: 14px;
        border:1px solid rgba(255,255,255,0.14);
        background:rgba(255,255,255,0.04);
        box-shadow:0 8px 25px rgba(0,0,0,0.22);
        font-weight:700;
        font-size: 14px;
        transition: transform .12s ease, background .12s ease, box-shadow .12s ease, border-color .12s ease;
      }
      .btn:hover{
        transform: translateY(-1px);
        background: rgba(255,255,255,0.06);
        box-shadow: 0 14px 34px rgba(0,0,0,0.30);
        border-color: rgba(255,255,255,0.20);
      }
      .btn:active{ transform: translateY(0px); }
      .btn:focus-visible{
        outline:none;
        box-shadow: var(--focus), 0 14px 34px rgba(0,0,0,0.30);
      }

      .btn.primary{
        border-color: rgba(9,255,3,0.45);
        background: linear-gradient(180deg, rgba(9,255,3,0.22), rgba(9,255,3,0.10));
        box-shadow:
          0 0 0 1px rgba(9,255,3,0.22) inset,
          0 14px 34px rgba(9,255,3,0.16);
      }
      .btn.primary:hover{
        border-color: rgba(9,255,3,0.62);
        background: linear-gradient(180deg, rgba(9,255,3,0.34), rgba(9,255,3,0.16));
        box-shadow:
          0 0 0 1px rgba(9,255,3,0.36) inset,
          0 18px 44px rgba(9,255,3,0.24);
      }

      /* Footer */
      .footer{
        border-top: 1px solid rgba(255,255,255,0.10);
        background: rgba(7,10,15,0.40);
        backdrop-filter: blur(12px);
      }
      .foot{
        display:flex;
        justify-content:space-between;
        gap:12px;
        padding: 16px 0;
        flex-wrap:wrap;
      }
      .muted{ color: var(--muted2); }

      /* Responsive */
      @media (max-width: 980px){
        .grid3{ grid-template-columns: 1fr; }
        .grid2{ grid-template-columns: 1fr; }
        h1{ font-size: 44px; }
        .hero{ padding-top: 34px; }
      }

      @media (max-width: 820px){
        .links{ display:none; }
        .menu{ display:block; }
      }

      /* Reduced motion */
      @media (prefers-reduced-motion: reduce){
        *{ transition: none !important; scroll-behavior: auto !important; }
      }
    </style>
  </body>
</html>
